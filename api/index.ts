// Vercel serverless entry — @vercel/node compiles this TypeScript file directly.
// Importing from src/ means no pre-built dist/ is required on Vercel.
import app from '../src/index';

export default app;
